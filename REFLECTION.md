1) First, the browser looks up the website's IP address using DNS. DNS servers translate the domain name into an IP address.
After getting the IP address, the browser connects to GitHub's server and establishes a secure HTTPS connection. It then sends an HTTP request asking for
the website files.

2) Docker containers have flexible, customizable, and isolated environments for running full applications, but GitHub pages are simpler.
They are a serverless platform that's designed for hosting static content without a backend function.
